{
  "name": "dsh-desktop",
  "version": "1.0.0",
  "description": "DeepSeek Harness 桌面版（自包含）",
  "main": "main.js",
  "author": "zhang",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . DeepSeekHarness --platform=win32 --arch=x64 --out=dist --overwrite --electron-version=31.7.7 --app-version=1.0.0 --no-asar --no-prune --ignore=\"\\.dsh-[A-Za-z0-9_]*|\\.npm-cache\""
  },
  "devDependencies": {
    "electron": "^31.7.7",
    "electron-packager": "^17.1.2"
  }
}
